import Blockchain from "./components/Blockchain";

const Home = () => {
  return (
    <div style={{ display: "flex", justifyContent: "center", background: "" }}>
      <Blockchain />
    </div>
  );
};

export default Home;
